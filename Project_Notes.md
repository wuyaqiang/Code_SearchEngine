#### 代码相似性的四种类型：
Type1: 除了空格、格式、注释的差别之外，其他的都应该保持相同。
Type2: 类型1的差别，加上标识符名称和变量值的差别， 其他的应该相同
Type3: 类型1和类型2的差别，加上语句级别的一些差异，如语句的增加、减少、修改等
Type4: 可能包含以上所有差异，即语法上不相似的代码，但是 实现的是同一种功能。这种相似性往往称之为语义相似性代码。



#### 4月2日 需求：

1. 设计算法，输入两个代码，判断两个代码之间的相似性，输出一个得分值，或设置阈值，输出相似或不相似。
2. 判断相似性之后，进一步找出原始代码中，哪一部分是相似的。